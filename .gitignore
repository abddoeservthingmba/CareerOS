# Secrets never enter the repo (CLAUDE.md).
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Flutter, for the phases that add them
node_modules/
dist/
build/
.dart_tool/

# Editors and OS
.DS_Store
.idea/
*.swp

# Web build output and dependencies
apps/web/node_modules/
apps/web/dist/

# `FOUND-13` — the generated clients are build outputs, reproducible from
# `packages/contracts/openapi.json`. A hand-committed copy becomes the version
# everyone actually uses and drifts from the server silently. `openapi.json`
# itself *is* committed: it is the baseline an API diff is taken against.
packages/contracts/ts/
packages/contracts/dart/

# Vite's dev-server cache. Written by `npm run dev`, never by a build, and it
# holds pre-bundled dependencies - so committing it would put a copy of
# node_modules in the repository.
apps/web/.vite/
