[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers --strict-config
asyncio_mode = auto
markers =
    real_ai: hits a real AI provider; nightly only (05-ai-layer.md §7)
    real_source: hits a real job source; nightly only (06-connectors.md §5.3)
